# 一等函数

> 命令式和函数式语言

编程语言中的一等对象：

1. 运行时创建
2. 可赋值给变量或数据结构中的元素
3. 能作为参数传递给函数
4. 能作为函数的返回结果

在Python中，所有函数都是**一等对象**。

高阶函数：接收函数作为参数，或者把函数作为结果返回的函数

> 内置的归约函数: `sum`、`all`和`any`

Python语言中的可调用对象：

- 用户定义的函数
- 使用C语言实现的内置函数
- 内置方法
- 类定义体中的方法
- 类
- 类实例
- 生成器函数


